namespace Gazeus.DesafioMatch3.Enums
{
    // Enum to avoid using -1 directly, improving code clarity.
    public enum TileType
    {
        Empty = -1,
        Red = 0,
        Blue = 1,
        Green = 2,
        Yellow = 3
    }
}
